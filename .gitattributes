# Enforce consistent LF line endings across platforms
* text=auto eol=lf

# Common binary patterns (no EOL normalization)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico  binary
*.pdf  binary

# Keep lockfiles normalized as text (still LF)
package-lock.json text eol=lf
